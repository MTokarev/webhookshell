name: .NET

on:
  push:
    branches: [ "master" ]


jobs:
  framework-specific:
    uses: ./.github/workflows/dotnet.yml
    with:
      runtime-name: framework-dependent
      version-tag: ${{ github.run_number }}