name: .NET

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: |
        dotnet build --no-restore
        dotnet publish -c Release
    - name: Upload Build Artifact
      uses: actions/upload-artifact@v3
      with:
        name: artifact
        path: /home/runner/work/webhookshell/webhookshell/bin/Release/net6.0/publish/

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download Artifact
      uses: actions/download-artifact@v3
      with:
        name: artifact
    - name: Create a release
      id: new-release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.run_number }}
        release_name: Release ${{ github.run_number}}
    - name: Upload Artifact
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.new-release.outputs.upload_url }}
        asset_path: /home/runner/work/webhookshell/webhookshell
        asset_name: WebhookShell-${{ github.run_number }}.zip
        asset_content_type: application/zip